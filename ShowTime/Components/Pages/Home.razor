@page "/"

<PageTitle>ShowTime - Sophisticated Music Festival Management</PageTitle>

<div class="hero-section">
    <div class="container">
        <div class="row align-items-center min-vh-80">
            <div class="col-lg-6">
                <div class="hero-content fade-in">
                    <h1 class="display-3 fw-bold mb-4" style="color: #FEFDF9; text-shadow: 0 4px 8px rgba(0,0,0,0.3);">
                        Welcome to <span style="color: #E53E3E; text-shadow: 0 2px 4px rgba(229, 62, 62, 0.5);">ShowTime</span>
                    </h1>
                    <p class="lead mb-5" style="color: #F5F5DC; font-size: 1.25rem; line-height: 1.7; opacity: 0.9;">
                        The most sophisticated platform for discovering and managing music festivals and bands. 
                        Connect with extraordinary artists and create unforgettable experiences with elegance and precision.
                    </p>
                    <div class="d-flex gap-4 flex-wrap">
                        <a href="/festivals" class="btn btn-primary btn-lg px-5 py-3" style="font-weight: 600; letter-spacing: 0.025em;">
                            <i class="fas fa-calendar-alt me-3"></i>Explore Festivals
                        </a>
                        <a href="/getbands" class="btn btn-secondary btn-lg px-5 py-3" style="font-weight: 600; letter-spacing: 0.025em;">
                            <i class="fas fa-users me-3"></i>Discover Bands
                        </a>
                    </div>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="hero-visual text-center slide-up">
                    <div class="hero-icon-container">
                        <i class="fas fa-music hero-icon"></i>
                        <div class="hero-accent-1"></div>
                        <div class="hero-accent-2"></div>
                        <div class="hero-accent-3"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .page-content {
        background: none !important;
        margin-top: 0 !important;
        min-height: auto !important;
    }

    .main-content {
        padding: 0 !important;
        min-height: auto !important;
    }

    .container-fluid {
        padding: 0 !important;
        max-width: none !important;
    }

    .hero-section {
        background: linear-gradient(135deg, #1c0610 0%, #2F3349 50%, #3A1F2A 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        position: relative;
        overflow: hidden;
        margin-top: 76px; /* Account for fixed navbar */
    }
    
    .hero-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="0.5" fill="rgba(245,245,220,0.05)"/><circle cx="75" cy="75" r="0.3" fill="rgba(245,245,220,0.03)"/><circle cx="50" cy="10" r="0.4" fill="rgba(245,245,220,0.04)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
        opacity: 0.6;
    }
    
    .min-vh-80 {
        min-height: 80vh;
    }
    
    .hero-icon-container {
        position: relative;
        display: inline-block;
    }
    
    .hero-icon {
        font-size: 18rem;
        color: rgba(245, 245, 220, 0.08);
        animation: float 4s ease-in-out infinite;
        position: relative;
        z-index: 2;
    }
    
    .hero-accent-1, .hero-accent-2, .hero-accent-3 {
        position: absolute;
        border-radius: 50%;
        animation: pulse 3s ease-in-out infinite;
    }
    
    .hero-accent-1 {
        width: 100px;
        height: 100px;
        background: radial-gradient(circle, rgba(229, 62, 62, 0.2) 0%, transparent 70%);
        top: 20%;
        left: -20%;
        animation-delay: 0s;
    }
    
    .hero-accent-2 {
        width: 150px;
        height: 150px;
        background: radial-gradient(circle, rgba(112, 128, 144, 0.15) 0%, transparent 70%);
        bottom: 10%;
        right: -30%;
        animation-delay: 1s;
    }
    
    .hero-accent-3 {
        width: 80px;
        height: 80px;
        background: radial-gradient(circle, rgba(74, 44, 58, 0.2) 0%, transparent 70%);
        top: 60%;
        left: 80%;
        animation-delay: 2s;
    }
    
    @@keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-30px) rotate(5deg); }
    }
    
    @@keyframes pulse {
        0%, 100% { transform: scale(1); opacity: 0.6; }
        50% { transform: scale(1.2); opacity: 0.3; }
    }
    
    .features-section {
        background: linear-gradient(145deg, rgba(254, 253, 249, 0.98) 0%, rgba(245, 245, 220, 0.95) 100%);
        backdrop-filter: blur(20px);
        position: relative;
    }
    
    .py-6 {
        padding-top: 5rem;
        padding-bottom: 5rem;
    }
    
    .mb-6 {
        margin-bottom: 4rem;
    }
    
    .feature-card {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        border: none;
        position: relative;
    }
    
    .feature-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(90deg, #E53E3E 0%, #4A2C3A 50%, #708090 100%);
        border-radius: 1.5rem 1.5rem 0 0;
        opacity: 0;
        transition: opacity 0.3s ease;
    }
    
    .feature-card:hover::before {
        opacity: 1;
    }
    
    .feature-card:hover {
        transform: translateY(-15px) scale(1.02);
    }
    
    .feature-icon i {
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .feature-card:hover .feature-icon i {
        transform: scale(1.15) rotate(5deg);
    }
    
    .stats-section {
        background: rgba(245, 245, 220, 0.3);
        backdrop-filter: blur(20px);
        border-top: 1px solid rgba(112, 128, 144, 0.2);
        border-bottom: 1px solid rgba(112, 128, 144, 0.2);
    }
    
    .stat-item {
        transition: all 0.3s ease;
        padding: 2rem 1rem;
        border-radius: 1rem;
    }
    
    .stat-item:hover {
        background: rgba(255, 255, 255, 0.6);
        transform: translateY(-5px);
    }
    
    .cta-section {
        background: linear-gradient(135deg, #2F3349 0%, #4A2C3A 50%, #3A1F2A 100%);
        position: relative;
        overflow: hidden;
    }
    
    .cta-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="cta-grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="30" cy="30" r="0.5" fill="rgba(229,62,62,0.1)"/><circle cx="70" cy="70" r="0.3" fill="rgba(245,245,220,0.05)"/><circle cx="15" cy="80" r="0.4" fill="rgba(112,128,144,0.08)"/></pattern></defs><rect width="100" height="100" fill="url(%23cta-grain)"/></svg>');
        opacity: 0.4;
    }
    
    .btn-outline-light {
        border-color: rgba(245, 245, 220, 0.8) !important;
        color: #F5F5DC !important;
    }
    
    .btn-outline-light:hover {
        background-color: #F5F5DC !important;
        color: #2F3349 !important;
        border-color: #F5F5DC !important;
        transform: translateY(-3px);
        box-shadow: 0 8px 16px rgba(245, 245, 220, 0.3) !important;
    }
    
    @@media (max-width: 768px) {
        .hero-icon {
            font-size: 10rem;
        }
        
        .display-3 {
            font-size: 2.5rem;
        }
        
        .display-4 {
            font-size: 2rem;
        }
        
        .py-6 {
            padding-top: 3rem;
            padding-bottom: 3rem;
        }
        
        .hero-section {
            min-height: 100vh;
            margin-top: 56px; 
        }
        
        .feature-card .card-body {
            padding: 2rem !important;
        }
    }
</style>
